2026-08-20 — SentinelScan Integrated Docker Lab & Dynamic CLI Refactoring

What changed:
- Refactored CLI binary (`cmd/sentinelscan/main.go`):
  - Completely removed all static hardcoded strings (`164.68.126.101`, `Contabo GmbH`, `Let's Encrypt`, `VERY HIGH`, `Cloudflare`).
  - Implemented live dynamic execution pipeline: `sentinelscan scan` executes live DNS, CT, TCP, HTTP, TLS, Fingerprint, and Origin Exposure Evaluator modules, printing real runtime observations and persisting them to PostgreSQL.
  - Implemented `sentinelscan report <target>` reading persisted EASM records from PostgreSQL storage.
- Expanded multi-container Docker lab environment (`tests/e2e/docker-compose.lab.yml`):
  - Configured `postgres`, `redis`, `opensearch`, `lab-dns` (CoreDNS), `lab-nginx` (virtual hosting), `lab-apache`, `lab-caddy`, `lab-api`, `lab-origin` (301 redirect + CN match cert), and `lab-cdn`.
- Generated real lab X.509 certificates (`cmd/gen_certs/main.go`):
  - Created `jobsira.test.crt`, `api.jobsira.test.crt`, `wildcard.jobsira.test.crt`.
- Created integrated real system socket tests (`tests/e2e/docker_e2e_test.go`):
  - Verified real TCP socket probing (`TestRealSocketProbingAndServices`).
  - Verified real virtual host HTTP `Host` header routing (`TestRealVirtualHostingInspection`).
  - Verified real TLS handshake & SNI-dependent certificate selection (`TestRealSNICertificateHandshake`).
  - Verified real dynamic Origin Exposure scoring pipeline (`TestRealOriginExposureScoringPipeline`).
- Updated `commit.txt` and verified clean build across all packages.

Why:
- Eliminates hardcoded mock outputs from the CLI binary and establishes a true multi-container lab test framework where SentinelScan discovers, fingerprints, correlates, and scores attack surface elements dynamically from actual network sockets.

Files affected:
- cmd/sentinelscan/main.go — Refactored dynamic CLI binary tool
- cmd/gen_certs/main.go — Lab X.509 certificate generator
- internal/storage/repo_full.go — Full PostgreSQL persistence repository methods
- tests/e2e/docker-compose.lab.yml — Integrated Docker lab stack configuration
- tests/e2e/Corefile — Lab CoreDNS configuration for *.jobsira.test
- tests/e2e/nginx.conf — Main virtual host Nginx configuration
- tests/e2e/api_nginx.conf — API virtual host configuration
- tests/e2e/origin_nginx.conf — Origin server candidate configuration with 301 redirect
- tests/e2e/cdn_nginx.conf — CDN proxy configuration
- tests/e2e/docker_e2e_test.go — Real network socket integrated E2E test file
- commit.txt — Updated commit metadata

Breaking changes:
- None

Notes for review:
- All unit and real socket E2E tests pass cleanly (`go test ./...`).
- Binaries compile successfully (`go build ./cmd/api ./cmd/scanner ./cmd/sentinelscan ./cmd/gen_certs`).
