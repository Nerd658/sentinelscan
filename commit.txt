2026-08-20 — SentinelScan Phase 9 True Docker E2E System Test Suite

What changed:
- Implemented `TestTrueDockerE2ESystemChain` in `tests/e2e/true_docker_e2e_test.go`:
  - Enforces zero manual observation struct instantiation rule (all observations created dynamically by executing TCP, TLS, and HTTP probes against live network sockets).
  - Verifies dynamic TLS handshake, X.509 certificate extraction, HTTP 301 Location header parsing, technology fingerprinting, and origin exposure scoring pipeline.
- Implemented `TestAntiHardcodingDynamicServerHeaderChange` to verify that server header changes dynamically propagate to fingerprinting without code modification.
- Fixed API host header assertion bug in `tests/e2e/docker_e2e_test.go`.
- Added `lab-up`, `lab-down`, and `test-e2e-docker` Makefile targets.
- Verified clean build and test execution across all packages (`go test ./...`).

Why:
- Fulfills Phase 9 requirement, validating that SentinelScan processes network observations dynamically from actual sockets without relying on hardcoded or manually constructed observation structs.

Files affected:
- tests/e2e/true_docker_e2e_test.go — True Docker E2E system test file
- tests/e2e/docker_e2e_test.go — Fixed API host assertion test
- Makefile — Added lab-up, lab-down, test-e2e-docker targets
- commit.txt — Updated commit metadata

Breaking changes:
- None

Notes for review:
- All unit, integration, and E2E tests pass cleanly (`go test ./...`).
- Binaries compile successfully (`go build ./cmd/api ./cmd/scanner ./cmd/sentinelscan ./cmd/gen_certs`).
