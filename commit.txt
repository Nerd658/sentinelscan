2026-08-20 — Dynamic Target Authorization in Scope Engine for Online Scans

What changed:
- Added `AddAllowedIP(ipStr string)` in `internal/scope/scope.go` to support dynamic runtime authorization.
- Updated `cmd/sentinelscan/main.go` to:
  - Parse direct IP targets and register them in ScopeRules.
  - Dynamically authorize all IPs discovered via DNS resolution of an authorized target domain.
- Verified compilation and test suite passing (`go test ./...`).

Why:
- Enables SentinelScan to scan real online targets (e.g. preproduction / staging websites) while strictly preventing scans against out-of-scope third-party assets.

Files affected:
- internal/scope/scope.go — Added AddAllowedIP method
- cmd/sentinelscan/main.go — Dynamic scope enrollment for resolved target IPs
- commit.txt — Updated commit metadata

Breaking changes:
- None
